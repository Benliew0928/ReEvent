package com.reevent.app.ui

/** Kept only so unused preview and archived mock UI sources continue to compile. Runtime uses typed routes. */
@Deprecated("Runtime navigation uses typed destinations in ReEventApp")
enum class ReEventScreen {
    Onboarding, SignIn, Home, Marketplace, AddResource, Passport, AiMatch, PartnerMap, Impact, Profile,
    ParticipantReturn, PartnerWorkbench
}
